# Production-oriented compose: only expose the web UI. DB and API stay on the internal network.
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build

services:
  db:
    ports: !reset []

  backend:
    ports: !reset []

  frontend:
    ports:
      - "${HTTP_PORT:-80}:80"
