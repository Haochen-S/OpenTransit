export const TOKEN_KEY = "opentransit_token";
export const SCHEDULE_STALE_MS = 60_000;

/** Set to true when login/register is ready for production. */
export const AUTH_ENABLED = false;

export const AUTH_PAUSED_MESSAGE = "Login and registration are temporarily paused — we're working on improvements.";
